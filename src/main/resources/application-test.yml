spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:postgresql://localhost:5433/taskify_test
    username: taskify
    password: taskify
  jpa:
    properties:
      hibernate:
        ddl-auto: validate
        format_sql: false
  flyway:
    enabled: false

taskify:
  datasource:
    pool:
      max-size: 4
      min-idle: 1
